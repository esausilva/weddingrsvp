<% if obj.errors.any? %>
  <div class='row'>
    <div clas='col-xs-8 col-xs-offset-2'>
      <div class='panel panel-danger normal-font'>
        <div class='panel-heading'>
          <strong><span class='panel-title'>
            <%= pluralize(obj.errors.count, 'error') %>
            prohibited this RSVP from being submitted:
          </span></strong>
          <div class='panel-body'>
            <ul>
              <% obj.errors.full_messages.each do |err| %>
                <li><%= err %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>